# Blueprint Render — déploiement complet (Web service Node + PostgreSQL).
# Importez ce dépôt dans Render via « New → Blueprint » en sélectionnant ce fichier.
# Renseignez ensuite les variables marquées `sync: false` dans le dashboard Render.

databases:
  - name: taskpulse-db
    databaseName: taskpulse
    user: taskpulse
    plan: free

services:
  - type: web
    name: taskpulse-saas
    runtime: node
    plan: free
    region: frankfurt
    buildCommand: npm install && npm run build
    startCommand: npm run start
    preDeployCommand: npx prisma db push --skip-generate
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: taskpulse-db
          property: connectionString
      - key: NODE_VERSION
        value: "20"
      - key: TASKPULSE_ADMIN_EMAIL
        sync: false
      - key: AUTH_SECRET
        sync: false
      - key: GEMINI_API_KEY
        sync: false